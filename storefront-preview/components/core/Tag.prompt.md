**Tag** — pill-shaped filter / category chip; selected state fills with T8 blue.

```jsx
<Tag selected onClick={fn}>Reformers</Tag>
<Tag onRemove={fn}>Under HK$10,000</Tag>
```

- `selected` fills blue. Pass `onRemove` to show a dismiss × (active-filter chips).
