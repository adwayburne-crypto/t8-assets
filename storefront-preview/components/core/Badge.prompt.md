**Badge** — compact status/metadata label in mono caps (NEW, SALE, IN STOCK, −20%).

```jsx
<Badge tone="accent" variant="solid">Sale</Badge>
```

- `tone`: neutral · brand · accent · success · warning · danger.
- `variant`: `soft` (tinted, default) · `solid`. Use `accent solid` for SALE flags on product cards.
