**PriceTag** — product price set in Archivo, with optional compare-at strikethrough.

```jsx
<PriceTag amount={8990} compareAt={10990} currency="HK$" size="lg" />
```

- When `compareAt` is set the live price turns coral to signal a markdown.
