**Select** — styled native dropdown matching Input metrics.

```jsx
<Select label="Sort by" options={['Featured','Price: low to high','Newest']} />
```

- Options may be plain strings or `{value,label}` objects. Sizes match Input.
