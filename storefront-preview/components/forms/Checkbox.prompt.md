**Checkbox** — square check control with label, for filters and consent.

```jsx
<Checkbox label="In stock only" onChange={fn} />
```

- Controlled via `checked` or uncontrolled via `defaultChecked`. `onChange` receives the next boolean.
